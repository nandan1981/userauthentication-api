package com.ecommerce.userauthentication.repository;

import java.util.Optional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;

import com.ecommerce.userauthentication.model.User;

//@Service
public class UserRepositoryImpl {
	
	
	/*
	 * private final UserRepository repository;
	 * 
	 * @Autowired public UserRepositoryImpl(UserRepository repository) {
	 * this.repository = repository; }
	 * 
	 * 
	 * public ResponseEntity<Optional<User>> getUserData(Long id){ return
	 * ResponseEntity.ok().body(repository.findById(id).or(null)); }
	 */
	
	

}
